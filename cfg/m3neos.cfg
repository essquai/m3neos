if not defined("SL") SL = "/" end
if not defined ("M3_BACKEND") M3_BACKEND = "C" end
if not defined("TARGET") TARGET = "x86_64" end
INSTALL_ROOT = (path() & SL & "..")
include(path() & SL & "cfg" & SL & TARGET)
